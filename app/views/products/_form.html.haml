= simple_form_for(@product) do |f|
  = f.error_notification

  .row
    .col-md-6
      .form-inputs
        = f.input :name
        = f.input :description
        = f.input :image
        = f.input :price
        = f.input :category_id

      .form-actions
        = f.button :submit
